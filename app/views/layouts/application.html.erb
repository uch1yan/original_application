<!DOCTYPE html>
<html>
  <head>
    <title>OriginalApplication</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>  
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mystery+Quest&family=Original+Surfer&family=Poiret+One&family=Ribeye+Marrow&display=swap" rel="stylesheet">  </head>
  
  <body>
      
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <% if user_signed_in? %>
          <%= link_to image_tag(asset_path 'logo.png'), homes_path,  class: "navbar-brand" %>
          <%#= link_to 'HOME', homes_path,  class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to '今日の様子', daily_conditions_path,  class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to '成長記録', growth_records_path,  class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to '予防接種', kid_vaccination_records_path,  class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'カレンダー', schedules_path,  class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'メモ', posts_path,  class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'アカウント', user_path(current_user),  class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'ログアウト', users_sign_out_path,  class: "nav-link" %>
            </li>
              <% if current_user.admin? %>
                <li class="nav-item">
                  <%= link_to '管理者画面', rails_admin_path,  class: "nav-link" %>
                </li>
              <% end %>
          <% end %>
      </div>
    </nav>

    <% if notice %>
      <p class="alert alert-notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-error"><%= alert %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
