<div class="wrapper">
  <div class="user_info">
    <h2>ユーザー情報 <i class="fa-solid fa-user"></i></h2>
    <p>
      <strong><%= t('.user_name') %></strong>:
      <%= @user.name %>
    </p>

    <p>
      <strong><%= t('.email') %></strong>:
      <%= @user.email %>
    </p>


    <% if current_user.families.first.present? %>
    <p>
      <strong><%= t('.family') %></strong>:
      <%= current_user.families.first.name %>
    </p>
    <% else %>
      <%= link_to t('.register_family'), new_family_path%><br>
    <% end %>

    <button class="edit"><%= link_to t('.edit'), edit_user_path(@user) %></button>
  </div>
    <p><i class="fa-regular fa-face-smile"></i><%= link_to t('.register_my_kids'), new_kid_path %>   /
    <i class="fa-regular fa-envelope"></i><%= link_to t('.invite_family'), new_user_invitation_path %></p>